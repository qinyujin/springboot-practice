package runtime;

/**
 * @author :覃玉锦
 * @create :2021-01-13 22:09:00
 * 默认参数： -Xms9m -Xmx9m
 * 查看gc日志参数：还是一样的，查看gc详情 -XX:+PrintGCDetails
 */
public class GCTest {
    public static void main(String[] args) {
        /*int i = 0;
        try {
            List<String> list = new ArrayList<>();
            String a = "atguigu.com";
            while (true){
                list.add(a);
                a = a+a;
                i++;
            }
        } catch (Throwable t) {
            t.printStackTrace();
            System.out.println("遍历次数为:"+i);
        }*/
    }
}
